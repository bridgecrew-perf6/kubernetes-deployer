kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  namespace: default
  name: deployer
rules:
- apiGroups: ["", "extensions", "apps", "networking.k8s.io"]
  resources: ["services", "deployments", "ingresses", "replicasets", "pods", "configmap"]
  verbs: ["*"]
